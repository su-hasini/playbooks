---
- name: impementing host scope variables
  hosts: 172.31.87.74
  tasks: 
    - name: create user
      user: 
        name: "{{a}}"
        password: "{{b}}"
        uid: "{{c}}"
        home: "{{d}}"
        shell: "{{e}}"
...
